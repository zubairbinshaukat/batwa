{
  "name": "batwa-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zero-knowledge Cloudflare Worker relay for Batwa shared spaces",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test --test-concurrency=1 test/relay.test.mjs test/client.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
