# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Internal planning docs and the original build spec (not part of the product)
docs/
batwa-app-spec.md

# Tooling that must never land in the repo (Batwa has no npm dependencies)
node_modules/
package.json
package-lock.json
pnpm-lock.yaml
yarn.lock

# ...except the relay Worker, which is a separate deployable with its own
# toolchain (wrangler as a devDependency). Its node_modules stays ignored.
!relay/package.json
!relay/package-lock.json
relay/.wrangler/

# Local scratch
*.log
tmp/
.tmp/
